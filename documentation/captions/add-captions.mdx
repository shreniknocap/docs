---
title: "Add Captions"
icon: "closed-captioning"
---

Adding captions makes your video more engaging and easier to watch. Within the app, there are two easy ways to add captions to your video:

- Automatically, using AI
- Manually, by adding new words and phrases

<div id="ios-tab">
    <Tabs>
        <Tab title="iOS">
            **Automatically add captions to your video**

            1. Import or record your video: [link to import and camera]
            2. Select the language that is being spoken
            3. Keep “No Translation” or select a language to translate captions

            [GIF]

            **Manually add captions**

            - Scroll to a location where there are no captions
            - Tap ********Edit********
            - Tap ******Add******
            - Add your word(s) and apply
        </Tab>

        <Tab title="Desktop">
            **Automatically add captions to your video:**

            1. Import your video [link]
            2. Select the language that is being spoken
            3. Keep “No Translation” or select a language to translate captions

            [GIF]

            **Manually add captions**

            Coming soon to desktop
        </Tab>
    </Tabs>
</div>


<div id="desktop-tab">
    <Tabs>
        
        <Tab title="Desktop">
            **Automatically add captions to your video:**

            1. Import your video [link]
            2. Select the language that is being spoken
            3. Keep “No Translation” or select a language to translate captions

            [GIF]

            **Manually add captions**

            Coming soon to desktop
        </Tab>

        <Tab title="iOS">
            **Automatically add captions to your video**

            1. Import or record your video: [link to import and camera]
            2. Select the language that is being spoken
            3. Keep “No Translation” or select a language to translate captions

            [GIF]

            **Manually add captions**

            - Scroll to a location where there are no captions
            - Tap ********Edit********
            - Tap ******Add******
            - Add your word(s) and apply
        </Tab>
    </Tabs>
</div>


<script>
  // Function to hide or show elements by id
  function displayElement(id, show) {
      var element = document.getElementById(id);
      if (element) {
          element.style.display = show ? 'block' : 'none'; // Use 'block' for div elements
      }
  }

  // This will run immediately when the script is parsed, which might be before the DOM is fully ready.
  // Check if the user is on an iOS device
  var isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent) && !window.MSStream;

  // Show or hide divs based on the device
  if (isIOS) {
      // Use a timeout to give the browser a moment to ensure the DOM is fully loaded
      setTimeout(function() {
          displayElement('ios-tab', true);      // Show iOS tab
          displayElement('desktop-tab', false); // Hide Desktop tab
      }, 0);
  } else {
      // Use a timeout to give the browser a moment to ensure the DOM is fully loaded
      setTimeout(function() {
          displayElement('ios-tab', false);     // Hide iOS tab
          displayElement('desktop-tab', true);  // Show Desktop tab
      }, 0);
  }
</script>

